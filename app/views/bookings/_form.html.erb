<%= simple_form_for [garden, booking] do |f| %>
<%= f.input :start_date_time, label: 'From', as: :datetime %>
<%= f.input :end_date_time, label: 'To', as: :datetime %>
<%= f.submit class: 'btn btn-flat' %>
<% end %>
